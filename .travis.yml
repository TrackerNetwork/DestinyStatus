language: php
cache:
  yarn: true
  directories:
    - node_modules
php:
  - '7.1'
  - '7.0'
  - '5.6'
before_install:
  - npm install -g gulp
  - npm install -g yarn
  - yarn install
install:
  - composer install
  - php artisan key:generate
  - gulp
script:
  - ./vendor/bin/phpunit
